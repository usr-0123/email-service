namespace EmailPlatform.BuildingBlocks.Messaging;

// Skeleton — Phase 3 will add:
//   * MassTransit configuration (AddPlatformMessaging) wiring RabbitMQ + retries + outbox
//   * IIntegrationEventPublisher abstraction
//   * IOutboxRepository abstraction (Mongo-backed in Persistence.Mongo)
//   * In-process outbox dispatcher hosted service
internal static class MessagingPlaceholder
{
}
