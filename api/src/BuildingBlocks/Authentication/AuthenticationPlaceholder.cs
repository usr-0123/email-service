namespace EmailPlatform.BuildingBlocks.Authentication;

// Skeleton — Phase 1 will add:
//   * JwtBearerSetup (extension to configure JWT validation against Identity.Api)
//   * ApiKeyAuthenticationHandler (custom AuthenticationHandler<T> for X-Api-Key)
//   * AddPlatformAuthentication() composite registration
internal static class AuthenticationPlaceholder
{
}
