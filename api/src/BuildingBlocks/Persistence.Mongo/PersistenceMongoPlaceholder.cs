namespace EmailPlatform.BuildingBlocks.Persistence.Mongo;

// Skeleton — Phase 1 will add:
//   * MongoOptions (connection string, database-name resolution per service)
//   * MongoDbContext base class with conventions (camelCase, ignore-extras, GUIDs as strings)
//   * TenantScopedRepository<T> that injects tenantId filter on all reads/writes
//   * IPlatformAdminRepository<T> for the deliberate-bypass case
//   * AddMongo() composite registration
internal static class PersistenceMongoPlaceholder
{
}
